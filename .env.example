# Puerto del servidor (Next.js lo lee automáticamente)
PORT=3000

# Ruta al archivo SQLite (relativa al proyecto)
DATABASE_PATH=db/mimins.db

# Prisma (usado por prisma CLI para migraciones)
DATABASE_URL="file:./db/mimins.db"
