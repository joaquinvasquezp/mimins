# Ruta al archivo SQLite (relativa al proyecto)
DATABASE_PATH=db/dev.db

# Prisma (usado por prisma CLI para migraciones)
DATABASE_URL="file:./db/dev.db"

# Autenticación (solo necesario en producción)
# Generar SESSION_SECRET con: openssl rand -base64 32
AUTH_USER=
AUTH_PASSWORD=
SESSION_SECRET=
